export function setLightByTurn(isPlayerTurn) {
  // Por enquanto, apenas um placeholder para o sistema de iluminação
  // Aqui seria implementado o sistema de iluminação 3D
  console.log(`Iluminação alterada para: ${isPlayerTurn ? 'jogador' : 'oponente'}`);
}