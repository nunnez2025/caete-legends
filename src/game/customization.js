export const customizationSystem = {
  getCustomizedCard(baseCard) {
    // Por enquanto, retorna a carta base sem modificações
    // Aqui seria implementado o sistema de customização
    return { ...baseCard };
  }
};